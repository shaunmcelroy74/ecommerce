<nav class="fixed w-full top-0 bg-gray-100 z-10 py-4">
  <div class="max-w-7xl mx-auto px-2 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-16">

      <!-- Left group: Shop, About, Contact -->
      <div class="flex items-center space-x-10">
        <%= link_to 'Shop', root_path,
              class: "text-gray-500 hover:underline py-2 rounded-md text-md font-medium" %>
        <%= link_to "About",   about_path,   class: "px-3 hover:underline" %>
        <%= link_to "Contact", contact_path, class: "px-3 hover:underline" %>
      </div>

      <!-- Center: Logo -->
      <div class="flex-shrink-0 flex items-center">
        <%= link_to root_path do %>
          <%= image_tag 'logo.png', alt: 'Logo', class: 'block lg:hidden h-20 w-auto' %>
        <% end %>
        <%= link_to root_path do %>
          <%= image_tag 'logo.png', alt: 'Logo', class: 'hidden lg:block h-20 w-auto' %>
        <% end %>
      </div>

      <!-- Right group: Cart and User Links -->
      <div class="flex items-center space-x-6">
        <!-- Cart Icon -->
        <%= link_to cart_path,
              class: "text-gray-500 hover:underline py-2 rounded-md text-md font-medium" do %>
          <i class="fa-solid fa-cart-shopping fa-lg"></i>
        <% end %>

        <!-- Devise Sign-in/Out -->
        <% if user_signed_in? %>
          <span class="text-gray-700">Hello, <%= current_user.first_name %></span>
          <%= link_to "My Profile", edit_user_registration_path,
                class: "px-3 hover:underline" %>
          <%= link_to "Logout", destroy_user_session_path,
                method: :delete,
                class: "px-3 hover:underline" %>
        <% else %>
          <%= link_to "Sign Up", new_user_registration_path,
                class: "px-3 hover:underline" %>
          <%= link_to "Login", new_user_session_path,
                class: "px-3 hover:underline" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
